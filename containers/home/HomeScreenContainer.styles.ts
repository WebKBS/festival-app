import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  pendingContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },

  emptyContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    paddingTop: 60,
  },

  emptyText: {
    fontSize: 16,
    color: "#666",
  },
});
